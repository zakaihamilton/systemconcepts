## 2024-03-01 - [Virtual List Re-render Issue] **Learning:** Inline objects for the `itemData` prop in `VariableSizeList` cause all child items (`SearchResultItem`) to re-render constantly during scrolling because the object reference changes on every render. **Action:** Always use `useMemo` for complex properties like `itemData` passed to react-window components to optimize scrolling performance.
